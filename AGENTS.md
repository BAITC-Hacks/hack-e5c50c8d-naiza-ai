# Аким на 5 часов

Симулятор уже собран. Сначала прочитай это, потом меняй код.

Балл считает только `simulate()` в `lib/engine`. Клиент и сервер вызывают одну функцию. Модель в `lib/ai` объясняет готовый журнал и не придумывает числа. Набор дороже 1000 даёт ошибку `OverBudget`, без вызова модели. Пустой бюджет не повышает AQoLS.

Каноны в `lib/engine/canon.ts`. После правок движка обязаны сойтись `npm test` и `npm run demo`:

- база без действий: Score 52.56, средний 56.86, пол 49.18, район nura, два провала
- пример методики: стоимость 95, Score около 56.5, синергия M10+M12, провалов 0
- набор дороже 100 не считается

Данные только из `data/city.json`, версия `astana-rules-v2`. Бюджет 100. Районов 5, показателей 10, мер 14. Районы условные. Интерфейс по-русски.

Не добавляй авторизацию, базу, GIS, Mapbox и второй способ посчитать балл. Ключ `XAI_API_KEY` остаётся на сервере. Модель совета по умолчанию `grok-4.7`, база `https://api.x.ai/v1`.

Проверка: `npm test`, затем `npm run demo`. Полный план и промпты сборки лежат в `docs/PLAN.md` и `docs/PROMPTS.md`.
